name: grokemon

services:
  gateway:
    build:
      context: ..
      dockerfile: docker/gateway/Dockerfile
    ports:
      - "8787:8787"
    environment:
      - PORT=8787
      - ADMIN_TOKEN=${ADMIN_TOKEN:-dev-admin-token}
      - MAX_INSTANCES=${MAX_INSTANCES:-10}
      - EMULATOR_IMAGE=${EMULATOR_IMAGE:-grokemon-emulator}
      - NETWORK_NAME=grokemon-net
      - ROM_PATH=${ROM_PATH:-}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - grokemon-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8787/health"]
      interval: 10s
      timeout: 5s
      retries: 3

networks:
  grokemon-net:
    name: grokemon-net
    driver: bridge
